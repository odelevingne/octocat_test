Explain what git clone does, in your own words

The git clone command takes a remote repository and creates a copy of it, giving you a new local repository. It creates a new folder on your machine, so you have to be sure not to duplicate folders.
